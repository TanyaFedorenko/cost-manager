export type UserInput = {
 id: string;
  description: string;
  amount:string|number;
  date: Date;
};
export type CostType = {
  id: string;
  date: Date;
  description: string;
  amount:string|number;
};